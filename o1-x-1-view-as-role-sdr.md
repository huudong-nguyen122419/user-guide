# O1.x.1 · View as role → SDR

> O1 · Your Ops workspace → O1.x · Edge cases

**Preview what the SDR sees, "View as role → SDR".** From your profile control you can switch the UI into the trimmed SDR view (no Contacts/Companies; just their lists + Inbox). Use it to check exactly what a rep will get before you hand a list over.

![View as role SDR](v3s-140-view-as-role-sdr.png)

*O1.x.1: View as role → SDR: the preview banner + the trimmed SDR menu.*
