# O4.x.2 · Select all matching

> O4 · Remove members → O4.x · Edge cases

**Select all matching (bulk remove).** Tick the **header checkbox** to grab the visible page, or use **Select all matching** to select every contact matching the current filter (not just the page) → **Remove from list** clears them in one go.
